import { SettingsContent } from "@/components/settings/settings-content"

/**
 * Settings Page
 * 
 * Uses: SettingsContent component
 * Layout: (dashboard)/layout.tsx provides sidebar and navigation
 * 
 * To troubleshoot:
 * 1. Check components/settings/settings-content.tsx for content issues
 * 2. Check app/(dashboard)/layout.tsx for layout issues
 */
export default function SettingsPage() {
  return <SettingsContent />
}
